# 指标卡

- 职责：展示标签、已格式化的指标值、可选变化说明和趋势样式。
- 入口：`index.ts` 导出 `MetricCard`。
- 约束：调用方负责领域计算、数值格式化与本地化；组件不理解策略或投资组合业务口径。
- 扩展：跨业务通用展示能力可加入本组件；特定业务状态样式应留在所属 slice。
- 验证：`pnpm run lint && pnpm run build`；出现真实交互时再独立确定组件测试方案。
